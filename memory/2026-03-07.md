# 2026-03-07

- Implemented wallet rebinding approval flow across frontend + admin + Firestore rules.
- Added `walletChangeRequests` helper module and auto-submit behavior in Web3 context.
- Added admin approval page `/admin/wallet-requests` with approve/reject and batch user update.
- Added admin sidebar entries for wallet requests and live settings.
- Replaced `/account/nft-avatar` mock logic with real NFT verification + selector flow.
- Unified top header action buttons and wallet trigger visual style.
- Full project `npm run typecheck` still fails due existing legacy syntax/encoding issues in unrelated files (notably `src/app/account/page.tsx` and `src/lib/types.ts`).
- Unified admin promotions payment panel to read/write settings/payment (same source as Settings UI).
- Connected homepage promo carousel to Firestore configs/home_carousel with publish toggle + delete slot workflow in admin promotions.
- Fixed checkout payment defaults (usdt) and repaired OrderConfirmDialog corrupted price tokens causing build failure.
- Verified with npm run build (pass).
