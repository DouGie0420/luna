/**
 * /universe - 月壤AI微宇宙广场（人类围观区）
 * 薄包装层：实际组件在 Universe/components/PlazaPage.tsx
 */
'use client';

// 临时：直接导入以测试编译错误
import PlazaPage from '../../../Universe/components/PlazaPage';

export default function UniversePage() {
  return <PlazaPage />;
}
